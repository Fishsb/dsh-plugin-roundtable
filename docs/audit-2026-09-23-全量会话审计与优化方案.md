# 圆桌会议项目 · 执行过程审计与优化方案（全量会话版 · 方案 ⟷ 验收成对）

> ## ✅ 落地状态（2026-09-23 · 用户授权「你自己决策」后已实施）
>
> **四项修复已全部落地并过真机验证**（详见 §11 落地记录）：
> · 册零 `connect` 双真相消除 —— 工具描述 + usage + status 三处按模式如实
> · 册一 `create` 设置卡门禁 + `planCardSkipped` 留痕与消费面
> · 册二 `close` 分歧处置提示（有讨论无决定时）
> · 预设闭环：设置卡补 `reasoning_effort` + 修掉 **talent_pool 路由恒真缺陷**（新发现）
>
> **判据**：`npm test` **329 pass / 0 fail**（基线 314 → 329，新增 15 条守卫，全部经变异翻红自证）；
> 产物重建并热重载，宿主副本 `lib/index.js` 含全部修复。
> **未做**：册三 `egalitarian` 标注（须拍板）、预设合并/新增（`repro`↔`verify` 合并与 `dispute` 新席仍属建议）。

> **审计对象**：本仓（`dsh-plugin-roundtable`，当前 v0.2.50）
> **样本**：**139 个会话全量扫描**（`C:\Users\lk\.dsh\sessions\**\session.v3.jsonl.zstd`，261.5 MB）
> → 其中 **31 个会话用过圆桌**、**1094 次圆桌工具调用**、**18 个会话真正开过会**
> → **28 场会议**（跨 5 个工作区全盘去重，含 `.roundtable/**/meeting.json` + `transcript.jsonl`）
> **态标签**：**决策态**（只出方案与判据，不改源码；施工须用户具名授权）
> **审计口径**：不采信自述，只认工具实证。每条结论**必附位置**，给不出即标「未验证」。
> **数据脚本**：`.audit/scan-sessions.mjs` · `.audit/scan-meetings.mjs`（只读，可复跑）

---

## 0. 一句话结论

圆桌**被真实、高频地使用**（31 会话 / 1094 次调用 / 28 场会议），且**近期质量在上升**（新机制被采纳：`round>0` 从早期 3/20 → 近期 8/8，工作项标签 0 → 52）。

但审计发现**三类「机制空转」**，且它们与该次会议亲自诊断守藏项目的病**同族**：

| 类别 | 机制 | 实测 | 性质 |
|---|---|---|---|
| **A. 被重构架空** | `connect` 连边 | **27/28 场是 orchestrated**，而该模式下 `charter.ts:33` **刻意不拼 edges**；`send_message` 也不校验边 ⇒ **边对专家与送达均不可见**。**因果铁证**：提交 `8088c2f`(9/19 16:41) 引入该语义后，边使用从 **113 → 13（−88%）** | **不是装饰，是被一次重构静默架空的历史机制** |
| **B. 可被绕过** | R1 设置卡 | **8/35 场（23%）未经用户确认就建会**（`create` 无前置门，只在 description 里"建议"） | 用户知情权被跳过的口子 |
| **C. 无判据** | 分歧处置 | 28 场仅 22 条 `decisions`；24 场**零 decisions** | 反对意见被如何处置**无任何对账** |

---

## 1. 全量实测（可复现）

### 1.1 使用面（139 会话全量）

```
含圆桌调用的会话        : 31 / 139
真正开过会（create）    : 18 个会话 · 35 次 create · 28 场存活会议
圆桌工具调用总数        : 1094
按工作区              : shoucang 20 · dsh-plugin-roundtable 6 · project-nav 2 · lk/FF 2 · prompt-enhancer 1
```

**工具调用频次（全量排序）**

| 排名 | 工具 | 次数 | 读法 |
|---|---|---|---|
| 1 | `send_message` | 274 | **绝对主力**——派单与转达 |
| 2 | `status` | 174 | 轮询密集（6.2 次/场） |
| 3 | `add_node` | 168 | **6.0 席/场**（28 场平均 5.4 席） |
| 4 | `connect` | **128** | ⚠ **却零功能作用**（见 §2-A） |
| 5 | `speak` | 92 | 专家发言 |
| 6 | `create` | 35 | —— |
| 7 | `next_round` | 32 | 1.14 次/场（多数会议只推 1 轮） |
| 8 | `summarize` | 32 | —— |
| 9 | `close` | 30 | **< create 35 ⇒ 5 场未关** |
| 10 | `plan_meeting` | 27 | ⚠ **< create 35 ⇒ 8 场绕过**（见 §2-B） |
| 11 | `request_decision` | 24 | 仅 12 个会话用过 |
| 12 | `kb_digest` | 9 | KB 中转机制低频 |
| 13 | `collect_review` | 3 | 针锋相对用过 1 场 |
| 14 | `set_budget` | 3 | 闭麦恢复 |
| 15 | `start_review` | 2 | —— |
| 16 | `remove_node` | 2 | 中途换席几乎不用 |
| 17 | `finish_review` / `export_review` / `disconnect` | 各 1 | —— |

### 1.2 会议面（28 场全量）

```
模式分布              : orchestrated 27 · egalitarian 0 · redteam 1
轮数分布              : {0:17, 1:4, 2:2, 3:2, 4:3}   → 至少 2 轮 7/28
走了轮次机制(round>0) : 11 场 | 绕过(round=0) : 17 场
planItems 带依赖      : 11 / 58 项
用 new:<preset> 拉席  : 4 次
workItem 标签         : 52（**全部集中在 round>0 的 11 场**；round=0 的 17 场为 0）
edges 合计            : 126（19/28 场有边）
decisions 合计        : 22（**仅 4 场有**；24 场零 decisions）
有 export.md          : 21/28
有 review.json        : 1/28
时长中位              : 30 分钟（max 678）
token 预算使用率中位   : 12.2%（max 27%）
```

### 1.3 **正向演化**（本审计最重要的正面发现）

| 指标 | 早期（9/16–9/18 · 20 场） | 近期（9/19–9/22 · 8 场） | 变化 |
|---|---|---|---|
| 走轮次机制 | 3 / 20 | **8 / 8** | ✅ 机制被采纳 |
| workItem 标签 | 0 | **52** | ✅ 对账生效 |
| 平均轮数 | 0.55 | **1.88** | ✅ 从"一遍过"到"多轮" |
| decisions | 13 | 9 | 持平（按场均值 0.65 → 1.13 ↑） |
| **edges** | **113** | **13** | ❌ **崩塌 −88%** |

⇒ **结论**：新机制（`next_round` + `work_item`）**确实被用户/主持人采纳并迁移过去了**；而 **`connect` 连边被自然淘汰**——不是因为难用，是因为**它本来就不起作用**（§2-A），用户在实战中发现了它的无用。

### 1.4 那次样本会议的机制实况（`容量门开关化审议`）

| 机制 | 使用 | 证据 |
|---|---|---|
| 8 席全上 | ✅ | `meeting.json` nodes=8 |
| `next_round` | ⚠ 1 次，**8 项全空依赖（一浪）** | `roundPlans[0].items` |
| `connect` | ❌ **0 边** | `edges: []` |
| `request_decision` | ❌ 0 调用 | 会话调用序列 |
| 主持人主动澄清 | ❌ 0 次 | 8 轮 user/message 全为**用户主动** |

**用户澄清成本**：用户在**第 6、7 轮**才说清"容量门 = 设置里几个设置容量的框"，而**会议已在第 3 轮结束**。主持人自认：

> "你说的「容量门」指**哪一组数值框**？…**全仓查无你的原话，只有转述**；若指后者，本方案改的地方**根本不是病灶**"

⇒ `req-spec`（需求质询）预设**存在**且**只上席 1 次**——专治该病的席位被跳过。

---

## 2. 根因（收敛为 3 条）

### R1 · 一次重构把 `connect` 静默架空了 —— 且**没有任何信号**

**实证链（三步全部可复现）**：

1. **语义层**：`src/charter.ts:33` 原文 —— "单线制下刻意**不拼** roster/edges —— 名册段本身就是'还有别人'的泄露面"。而 **27/28 场是 orchestrated（= 单线制）** ⇒ 专家**读到的总纲里根本没有边**。
2. **行为层**：`send_message` 的 `execute` 只校验三件事（`src/tools.ts:1114-1161`）：`work_item` 在本轮 plan 内（`:1124`）、模式策略 `recipientPolicy`（`:1138`）、收件人在节点名单（`:1158`）。**全程不读 `meeting.edges`**。
3. **统计层**：边使用 **113 → 13**，拐点与提交 `8088c2f`（`v0.2.37–v0.2.40：圆桌三模式语义`，2026-09-19 16:41 +0800）**同步**。

⇒ **定性**：`connect` **不是天生无用的装饰**，而是**被 2026-09-19 的三模式重构架空了**——重构把可见性判据集中到 `recipientPolicy`（按 mode），**边从"决定谁能发给谁"退化成"只给 UI 看的图"**，但：

- **工具描述没改**（`src/index.ts:110` 仍在教："Wire the topology … `forward` = pipeline hand-off, `bidirectional` = debate channel"），
- **`status` 输出没标**（`src/tools.ts:2123` 仍平铺列出 edges），
- **没有任何测试或判据覆盖"边是否还有用"**。

⇒ 主持人在被教着画一张**在这个模式下不产生任何效果**的图。**这是"拆东墙补西墙"的教科书形态**：新语义拆掉了边的职责，却把"画边"这个动作留在提示词里当装饰。

> **附带发现**：`egalitarian`（唯一真正消费 edges 的模式）**0 生产使用** ⇒ **边的唯一合法用途落在了一个没人用的模式上**。这才是"0 使用"与"113→13"两件事的共同解释。

### R2 · 「建议」不是「门」—— R1 设置卡有 23% 绕过率

**实证**：`roundtable_create` 无任何前置检查；"先调 `plan_meeting`"只写在 description 里（`src/tools.ts:380`，措辞为 `Preferred flow` / `Call it for EVERY meeting`）。

**实测绕过 8 次 / 35 次 create（23%）**：

```
情境维度体系重构会审 / 情境维度体系重构会审v2 / 情境轴去留裁决  (session-63555570-649)
domain-routing-v2                                            (session-87161bb5-4d7)
effort-e2e-probe                                             (session-b7ba3eec-62d)
r-d-live-verify / r-dui-verify / r-d-egalitarian-verify       (session-c18372a4-f07)
```

⇒ 用户**在不知情的情况下**会议已被创建（无设置卡 = 没有确认席位/预算/模式的机会）。这与本仓自称的 **R1 纪律**（"NEVER create a meeting straight away"）**直接冲突**。

⚠ **须诚实区分**：其中 3 场是插件作者自己的**验证会议**（`r-d-live-verify` 等），绕过很可能是**有意为之**（测 `create` 本身）。其余 5 场（含 `domain-routing-v2` 这样有 8 条边、21 行的真实会议）则**不是**。⇒ 判据应允许**显式**绕过（见册二），而不是一刀切禁止。

### R3 · 分歧处置无对账 —— 22 条 decisions / 28 场

**实证**：`decisions` 仅 4 场有值（`role-presets-self-review` 2 · `ui-优化会审` 4 · `project-nav-anticipate-duplication` 2 · `preset-effort-row` 3 …），**24 场为零**。

而那次样本会议**明确产出 5 条待拍板项**（O1/O2/O3/O4/O5 + ★最优先）——**全部写在方案正文里，未走 `request_decision`**。

⇒ 会议**产出了分歧，但没有"分歧被怎么处理"的收口判据**。主持人靠自觉把意见折进方案 ⇒ **反对意见静默丢弃 = 用户最担心的"拆东墙补西墙"的温床**。

---

## 3. 优化方案（按板块分册 · 方案 ⟷ 验收成对）

> **分册原则**：按依赖序；每册含「判因 / 做法 / 明确不做 / 验收（可机检 + 先红）」。
> **自律约束**：不新建模块、不加抽象层、不动冻结棘轮。每册验收须**先能红**（改前能证明判据有效）。

---

### 册零 · 消除 `connect` 的**双真相**（**P0 · 直击 R1**）

**判因**：三模式重构（`8088c2f`）把边的职责拆掉，但**提示词仍在教主持人画边**，`status` 仍在平铺展示边，**没有任何判据覆盖"边是否还有用"**。

⇒ 这是本仓自有的「**两种真相**」缺陷形态：**实现按 A（边不参与可见性）、文案按 B（边是协作拓扑）**。实测代价：18 场会议画了 122 条边，而专家**一个字都读不到**。

**做法（分模式，因为两种模式的病不同）**：

| 模式 | 现状 | 做法 |
|---|---|---|
| **orchestrated / redteam**（27/28 场） | 边既不进总纲、也不参与送达 ⇒ **零作用** | ① `roundtable_connect` 的工具描述**按当前模式**给出差异文案：单线制下明确写「边在本模式下**不参与送达、专家不可见**，仅用于主持人自述的协作图；如需表达先后关系，请用 `next_round` 的 `depends_on`」② 建会时 `status` 的 edges 段加注 `（装饰 · 不影响送达）` |
| **egalitarian**（0 场） | 边是**唯一**真正被消费的地方（总纲会拼 roster+edges，`charter.ts:41-55`） | **不动**。它是边语义的正当归属地 |

**为什么不做"给单线制的边加消费面"**：单线制的**定义**就是"专家互不披露"（`charter.ts:37`）⇒ 让边影响送达会**直接违反三模式语义**（用户 2026-09-19 定稿）。**正解是把残留的期待去掉，而不是把边硬塞回可见性**。

> **与用户主张的一致性**：用户主张"**主张信息皆可重构与统一压缩**、**反对冗余堆叠**"。本册不删 `connect`（egalitarian 需要它），而是**让文案与实现同源**——这正是"统一压缩"而非"再加一层"。

**验收**

| # | 判据 | 检查方式 | 阈值 | 先红证据 |
|---|---|---|---|---|
| C-1 | 边不参与送达（**现状确认，锁住不回归**） | 夹具：单线制 + **零边** ⇒ `send_message` 仍 `delivered=wake` | 必须成功 | 现状即如此；夹具缺失 ⇒ **本判据当前为"未验证"** |
| C-2 | 工具描述随模式差异化 | 断言 `connect` 的 description 在单线制下含"不参与送达" | 精确包含 | **改前不含** ⇒ 先红可证 |
| C-3 | `status` 边段带注 | 断言单线制下 edges 段含"不影响送达" | 精确包含 | 改前不含 ⇒ 先红 |
| C-4 | egalitarian 的边**仍进总纲** | 夹具：egalitarian 会议 ⇒ `buildCharter` 输出含 edges | 精确包含 | **现有测试可能有覆盖，须先查** |
| C-5 | 零回归 | `test/*.test.mjs` 中 mode/persona/charter 相关全绿 | 全绿 | —— |
| C-6 | 不新增模块 | `check-module-growth` 模块数不变 | 前后同值 | —— |
| C-7 | **新增**：边的消费面清单可查 | 文档/注释列出 edges 的**全部**消费方（charter/egalitarian · snapshot/UI · rpc 增删） | 三处齐全 | 改前**无此清单** ⇒ 先红 |

> ⚠ **C-1 是本册最重要的判据，且当前是空的**：全仓**没有任何夹具**断言"无边也能送达"（已 `grep test/` 确认，仅 `budget.test.mjs:18` / `persona.test.mjs:21` 的夹具里出现 `edges: []`，属数据而非断言）。⇒ 这条"边不影响送达"的语义**目前靠人工阅读源码维持**，任何一次重构都可能悄悄改掉它。

---

### 册一 · R1 设置卡：给 `create` 一个**可显式绕过**的门（**P0 · 直击 R2**）

**判因**：23% 绕过率；其中多数非测试用途。

**做法**（**不搞一刀切**，兼顾验证场景）：

`roundtable_create` 增参数 `skip_plan_card?: boolean`：

- **未传** 且本会话**没有**与该 `name` 配对的 `plan_meeting` 批准记录 ⇒ 抛出错误，提示"先调 `roundtable_plan_meeting`；若确为验证/脚本用途，显式传 `skip_plan_card: true`"。
- **传 `true`** ⇒ 放行，**并在 `meeting.json` 记 `planCardSkipped: true`**（留痕，可事后统计）。

**为什么是"显式绕过"而不是"硬门"**：插件作者需要写验证会议（实测 3 场），硬门会挡住正当用法；而**显式**绕过把"我知道我在跳过用户确认"变成一个**必须说出口的事实**——正是本仓自有的「留痕字段必须有消费面」纪律。

**配套消费面**：`roundtable_status` 增 `plan_card_skipped` 字段，使"这场会议没让用户确认过"**在事后可查**。

**验收**

| # | 判据 | 检查方式 | 阈值 | 先红证据 |
|---|---|---|---|---|
| P-1 | 未确认即建会 ⇒ 报错 | 夹具：直接 `create` 不带 `skip_plan_card` | 必须 throw | **现状不 throw** ⇒ 先红可证 |
| P-2 | 显式绕过 ⇒ 成功 + 留痕 | 夹具：带 `skip_plan_card:true` | `planCardSkipped===true` | 改前无此字段 ⇒ 先红 |
| P-3 | 正常流程零回归 | 既有 `plan` 相关夹具全绿 | 全绿 | —— |
| P-4 | 可事后统计 | `status` 含 `plan_card_skipped` | 布尔精确 | 改前无 ⇒ 先红 |
| P-5 | 报错文案可操作 | 断言含 `skip_plan_card: true` 字样 | 精确包含 | —— |

---

### 册二 · 分歧处置对账：新增 `dispute` 席 + 收口判据（**P1 · 直击 R3**）

**判因**：24/28 场零 `decisions`；反对意见的最终去向**无任何记录**。

**做法（两半，缺一不可）**：

**① 新增 1 席预设 `dispute`（争议归位）**

```yaml
- id: dispute
  name: 争议归位
  role: 只审查「分歧是否被真的处理」，不重新论证方案。逐条列出会上出现的反对意见与订正，裁为三类：已采纳（给落点）、已驳回（给理由与依据）、未处理。第三类必须逐条点名，不得遗漏；禁止把「没人再提」当成「已达成一致」。找不到未处理项就明确写「未发现」。产出：分歧处置表（意见 / 裁定 / 依据 / 落点）+ 未处理清单。
  provider: dshapi
  model: deepseek-v4.1-flash
```

> **为何 `converge` 不够**：`converge` 是**会上主动收敛**（过程），`dispute` 是**收口时以 transcript 为输入的事后对账**（判据）。一个在过程里，一个在结果上，职责不重叠（已按 `preset-audit` 的"近重复对 ≥0.30"口径自查，见验收）。

**② 收口判据**：`roundtable_close` 的返回文案，若 `decisions === 0` 且 `speak 数 ≥ 4`（说明有实质讨论），追加一行提示：

```
⚠ 本场有 N 条专家发言但 0 条 decisions —— 反对意见的去向未被记录。
   建议在收口时用 dispute 席做一次「分歧归位」，或把 [建议决策] 折进给用户的汇总。
```

> ⚠ **不阻断**（`close` 必须成功），且**只在"有讨论无决定"时触发**（避免噪音）。

**验收**

| # | 判据 | 检查方式 | 阈值 | 先红证据 |
|---|---|---|---|---|
| D-1 | 预设合规 | `node D:\lk\FF\scripts\preset-audit.mjs` | role 110–180 字 · 含「产出：」· name 无模型串 | —— |
| D-2 | 无近重复 | 同上「近重复对（≥0.30）」 | **= 0**（尤其与 `converge`） | 改前 `converge` 无对 ⇒ 加后有对即红 |
| D-3 | 统计口径 | `preset-index.md` 条数 | 28 → **29** | —— |
| D-4 | 收口提示触发 | 夹具：4 条 speak + 0 decisions ⇒ close 文本含提示 | 精确包含 | **改前不含** ⇒ 先红 |
| D-5 | 不误触发 | 夹具：0 decisions + 2 speak ⇒ **不含**提示 | 精确不含 | —— |
| D-6 | close 不阻断 | 提示存在时仍 `closed:true` | 必须成功 | —— |
| D-7 | decisions>0 时不提示 | 夹具：有 decision ⇒ 不含提示 | 精确不含 | —— |

---

### 册三 · `egalitarian` 模式：**标注实测，不加投入**（**P2 · 须拍板**）

**判因**：**0 / 28 场使用**。它是 v0.2.37–v0.2.40 的主要投入（"圆桌三模式语义"）。

| 选项 | 做法 | 评价 |
|---|---|---|
| A · 保留现状 | 不改 | 继续背复杂度，用户误以为它是成熟三等选项 |
| **B · 如实标注**（**推荐**） | README/设置页标注「实验性 · 实测 0/28 场使用」 | 诚实；代码保留（多模型辩论未来可能真需要） |
| C · 退役 | 删代码路径 | 大；且证据不足（0 使用可能是题目没遇到） |

> **已知反证**：会话 `c18372a4` 有一场 `r-d-egalitarian-verify`——**作者自己在验证它**。⇒ "0 使用"更准确的表述是「**除作者验证外，0 次生产使用**」。这削弱了 C 的正当性，也说明 B 的措辞要准。

**验收**

| # | 判据 | 检查方式 | 阈值 |
|---|---|---|---|
| E-1 | 文档如实 | README 模式段含"0/28 场生产使用" | 精确包含 |
| E-2 | 设置页提示 | 选 egalitarian 时出提示 | 人工走查 |
| E-3 | 零回归 | 既有模式语义夹具全绿 | 全绿 |

---

## 4. 角色预设调整方案（增 / 改 / 退 · 方案 ⟷ 验收成对）

### 4.1 上席实测（28 场全量 · 按 `presetId`）

```
高频（≥3）: accept 7 · arch 6 · impl 5 · minimal 4 · user 3 · rollout 3 · reuse 3
中频（1–2）: converge 2 · fact 2 · edge 2 · assumption 2 · verify 2
            · risk 1 · obs 1 · tradeoff 1 · req-spec 1 · scenario 1
从未上席(11): value · steelman · redteam · perf · security · data · deps · maint
            · repro · baseline · deliverable
```

### 4.2 处置（关键：区分「**该上没上**」与「**确实没用**」）

**① 该上没上 —— 修选席，不修预设**

| 席位 | 为什么该上 | 证据 |
|---|---|---|
| `security` | 主题涉及**凭据门 exit 5 / 密钥扫描 / 公开树脱敏** | 那次会议后确有 `test-secret-redact` 清理提交 |
| `data` | 主题涉及**存量处置 / 回滚 / 双写** | 方案 §7-O2「存量」专列 |
| `value` | 用户两次指令强调"**不要过度设计/过度解读需求**" | 本轮用户原话 |
| `req-spec` | **目标不清是最贵返工源** | §1.4 用户澄清发生在会议结束之后 |

⇒ **这四席一个字不改**，靠**选席提示**（见 4.3 可选册）把它们拉上台。

**② 确有重叠 —— 建议合并（−1 席）**

| 合并 | 理由 |
|---|---|
| `repro` + `verify` → 保留 `verify` | `verify` 原文含"证据不足即判**不可复现**"；`repro` 原文"**不能复现**的标未复现…不许改判为成立"——**同一判据的两种措辞**。`repro` 是 `verify` 的**输入**，不是独立视角 |

> ⚠ **唯一建议的合并**。其余 10 席虽零使用，但**职责互不重叠**（`perf`/`deps`/`maint`/`baseline`/`deliverable` 各管一域），合并会损失视角。

**③ 建议新增（+1 席）**：`dispute`（见册二 ①）

**④ 建议退役：0 席**
零使用 ≠ 无用。**不删**，避免"下次要用时没了"。除 `repro`（重叠）外全部保留。

**净效果**：**28 → 28 席**（−1 合并 +1 新增）。

### 4.3 预设验收

| # | 判据 | 检查方式 | 阈值 | 先红证据 |
|---|---|---|---|---|
| PS-1 | 新增席合规 | `node D:\lk\FF\scripts\preset-audit.mjs --write-index` | role 110–180 字 · 含「产出：」· name 无模型串 | —— |
| PS-2 | 无近重复 | 同上「近重复对（≥0.30）」 | = 0 | 改前无 `dispute` ⇒ 加入后有对即红 |
| PS-3 | 合并无悬挂引用 | `git grep repro` 于预设/文档 | 除历史记录外 0 命中 | —— |
| PS-4 | `verify` 吸收 repro 语义 | 人工读 `verify` role | 含"不可复现"裁定 | —— |
| PS-5 | 条数 | `preset-index.md` | 28 → **28** | —— |
| PS-6 | 路由一致 | 同上（新增席 provider/model） | `dshapi/deepseek-v4.1-flash` | —— |

---

## 5. 明确不做（防止本方案自身过度设计）

| # | 不做 | 理由 |
|---|---|---|
| N-1 | 不给 `connect` 加**硬**约束 | 单线制下边只对主持人可见，硬约束无信息价值 |
| N-2 | 不删零使用的 10 席 | 零使用是**题目分布**，不是预设缺陷 |
| N-3 | 不改 `plan` 必填门（2+ 席） | 用户 2026-09-19 已拍板；单席豁免合理 |
| N-4 | 不做「按题智能推荐阵容」 | 过度设计；册四的关键词提示（可选）已够 |
| N-5 | 不新建模块/抽象层 | 册零改描述、册一加参数、册二加预设，全落在既有文件 |
| N-6 | 不动 `maxRounds: 10` | 实跑 1 轮不是设置错，是用法；token 使用率仅 12.2% 说明**不是预算不够** |
| N-7 | 不改 `roundtable_close` 的"未派单"行为 | 17 场 round=0 **产出并不差**（2.7–12.9 万字符）⇒ 绕过机制**未必更劣**，先看数据 |

---

## 6. 执行序（硬依赖）

```
册零（connect 定性）── 无依赖
册一（设置卡门）──── 无依赖，可并行
册二（dispute 席+收口提示）── 无依赖
   ↓
册三（egalitarian 标注）── 须拍板
预设调整（4.2 合并 repro）── 独立
```

---

## 7. 风险栏（诚实边界）

| # | 风险 | 状态 |
|---|---|---|
| RK-1 | **17 场绕过轮次机制的会议产出可能更好**（12.9 万字符）⇒ 强制机制未必有益 | ⚠ **必须承认**。故册零/册一都只做**可见与留痕**，不做强制 |
| RK-2 | 册一的门可能挡住正当的快速建会（如"就问一个专家"） | 已用 `skip_plan_card` 显式绕过缓解；仍需真机走查 |
| RK-3 | `dispute` 席可能与主持人收口职责重叠 | 未验证。首次使用若重复 ⇒ 按 PS-2/PS-3 撤 |
| RK-4 | 28 场含 3 场作者验证会议（`effort-e2e-probe`/`preset-effort-row`/`r-d-*`） | 已识别；**未剔除**（剔除后 `egalitarian 0/28`、`round=0` 占比结论不变） |
| RK-5 | 全局设置 `maxRounds: 10` 与实测 1 轮差一个数量级 | 未验证是否有害；**不动**，先留观 |

---

## 8. 证据索引

| 结论 | 位置 |
|---|---|
| 139 会话扫描 / 31 会话用圆桌 / 1094 次调用 | `.audit/scan-sessions.mjs` → `.audit/scan.jsonl` |
| 28 场会议全量指标 | `.audit/scan-meetings.mjs` → `.audit/meetings.jsonl` |
| `send_message` 不校验 edges | `src/tools.ts:1114-1161` |
| 单线制**刻意不拼** roster/edges | `src/charter.ts:33`（含原文注释） |
| edges 真实消费方仅 3 处 | `src/charter.ts:41-55`（**仅 egalitarian**）· `src/snapshot.ts:128/235-253` + `src/client/wire.ts:322`（UI）· `src/rpc.ts:630-675`（增删自身） |
| 边被架空的拐点提交 | `8088c2f`（v0.2.37–v0.2.40 三模式语义，2026-09-19 16:41 +0800） |
| 工具描述仍在教画边（未随重构更新） | `src/index.ts:110` |
| 「边不影响送达」**当前无夹具覆盖** | `grep test/`：仅 `budget.test.mjs:18` / `persona.test.mjs:21` 出现 `edges: []`（数据非断言） |
| `create` 无前置门（仅 description 建议） | `src/tools.ts:380` |
| 8/35 次 create 未经设置卡 | §1.1 配对统计（本档 §2-R2 列出全部 8 例） |
| `next_round` 硬门只按席数 | `src/tools.ts:987-995` |
| work_item 校验在本轮 plan 内 | `src/tools.ts:1124-1134` |
| 那次样本会议 8 席/0 边/1 轮 | `.roundtable/容量门开关化审议/meeting.json` |
| 主持人自认"只有转述" | `docs/capacity-gate-adjustment-plan.md` §五 ★ |
| 预设机检口径 | `D:\lk\FF\preset-index.md`（脚本直出） |
| 预设全文 | `experts.yaml` / `EXPERTS.md` |

---

## 9. 与那次会议的同构（方法论自省）

那次会议诊断守藏项目的三条根因，**在本仓上原样复现**：

| 守藏那次会议的根因 | 圆桌本仓的对应形态 |
|---|---|
| R2 策略判定长在机制里，无单一事实源 | **一次重构拆掉了边的职责，却把"画边"留在提示词里** ⇒ 实现与文案两种真相（册零） |
| R3 失败/状态不可观测 | **23% 绕过设置卡、86% 零 decisions** 全程无信号（册一/册二） |
| R1 口径多处不统一 | "什么算一场有效会议"无判据：`round=0` 的 17 场**既不报错也不算空**；"边是否还有用"**无任何判据**（册零 C-1 当前为空） |

⇒ **本方案的第一性取向**：不修"专家不够聪明"、不修"提示词不够细"，而修「**机制被使用/被绕过/被架空之后，系统还能不能说真话**」。
这与用户"不要拆东墙补西墙"的要求一致——**补的是观测与诚实，不是补丁**。

---

## 10. 本审计自身的诚实地界

| # | 事项 | 状态 |
|---|---|---|
| H-1 | 我第一轮（仅 2 个 `.roundtable` 目录）得出"**connect 零使用**" | ❌ **已被自己的全量扫描证伪**（实际 128 次、19/28 场有边）。**保留此记录**以示样本偏差之害 |
| H-2 | 我第二轮得出"connect 是**纯装饰**" | ⚠ **已被第三轮修正**：它不是天生无用，是**被 8088c2f 架空**（语义层证据） |
| H-3 | 28 场统计含 3 场作者验证会议 | 已识别，**未剔除**（剔除后 `egalitarian 0/28`、拐点结论均不变） |
| H-4 | 「22 条 decisions」的口径 | 来自 `meeting.json.decisions` **数组长度**；用户侧的 `request_decision` 交互记在会话事件里（24 次调用），两者**不等价**，本档已分别标注 |
| H-5 | 册零 C-1 判据 | **当前为空**（无夹具）⇒ 属**新增**判据，不是"确认既有" |

---

## 11. 落地记录（2026-09-23 · 用户授权「你自己决策」后实施）

### 11.1 改了什么（4 处修复 + 1 处新发现）

| # | 修复 | 文件 | 性质 |
|---|---|---|---|
| ① | `connect` 双真相消除：工具描述标注 `MODE-DEPENDENT`、usage rule 4 改为「单线制下是装饰，别花调用」、`status` 的 Edges 段按模式如实标注 | `src/tools.ts` · `src/index.ts` | 文案与实现同源 |
| ② | `create` 设置卡门禁：未确认即拒绝；`skip_plan_card: true` 显式绕过并记 `planCardSkipped`；`status` 回吐该字段 | `src/tools.ts` · `src/types.ts` | 新增判据 + 留痕消费面 |
| ③ | `close` 分歧处置提示：≥3 位专家发言且 0 决策时出声（不阻断） | `src/tools.ts` | 新增可观测 |
| ④ | 设置卡补 `reasoning_effort`（显式 > 预设 > 继承），与 `add_node` 同序 | `src/plan.ts` · `src/tools.ts` | **预设闭环补链** |
| ⑤ | **新发现并修复**：`talent_pool` 路由渲染**恒真缺陷** | `src/tools.ts` | 见 §11.2 |

### 11.2 新发现：预设路由的恒真条件（真机复现）

**缺陷式**（修复前 `src/tools.ts` 的 `renderStatus`）：

```js
const route = String(candidate.provider ?? '') === '' || String(candidate.model ?? '')
  ? '（继承主持人路由）'
  : `${candidate.provider}/${candidate.model}`
```

`||` **右侧漏了 `=== ''`** ⇒ 它返回的是 model 字符串本身（truthy）⇒ **整个条件恒真**。

**后果（实测）**：`roundtable_status` 的 talent_pool 把 **28/28 条预设全部显示为「（继承主持人路由）」**，
而同一条预设在 `roundtable_list_presets` 里显示 `dshapi/deepseek-v4.1-flash` —— **同一份数据、两个工具、两个说法**。
主持人据此以为候选池没有路由信息，**在决策环节放弃按预设路由上席** ⇒ 预设自带的 provider/model 事实上失效。

**为什么既有测试抓不到**：既有断言只检查「字段进了 JSON」，而这是**渲染层字符串**（本档 §2-R3 的"两种真相"同族）。

**修法**：改为双向非空判定 `provider !== '' && model !== ''`，与 `list_presets` 的口径取反等价。

**真机证据**：修复前 `（继承主持人路由）`×28 → 修复后 `dshapi/deepseek-v4.1-flash`×28。

### 11.3 判据与验收（全部实跑）

| 项 | 结果 |
|---|---|
| 全量测试 | **329 pass / 0 fail / 0 skip**（基线 314） |
| 新增守卫 | `test/double-truth-guards.test.mjs` 15 条（①②③④⑤ 五组） |
| 变异自证 | 5 处逐一改坏 → 守卫**全部翻红**（12/1、12/1、12/1、12/1、13/2），还原后全绿 |
| 产物 | `npx tsdown` 重建成功；`lib/index.js` 经文本断言含全部 8 处标记 |
| 宿主副本 | junction 指向本仓 ⇒ 宿主加载的 `lib/index.js`（266 KB）含同样标记 |
| 热重载 | `dev_reload_package` → 清缓存 4 模块，fiber `active` |
| **真机端到端** | ①未确认建会 → **被拒绝**且报错给出绕过写法 ②`skip_plan_card:true` → 建会成功 + 警示 ③`status` 显示 `plan_card_skipped` ④Edges 标注「DECORATION in orchestrated」⑤talent_pool 路由 28/28 正确 ⑥`close` 在 0 发言时不误报 |
| 清理 | 4 个探针会议目录已删；审计中间产物已删（保留 2 个可复跑脚本 + 汇总 JSON） |

### 11.4 未做（老实交代）

| 项 | 原因 |
|---|---|
| 册三 `egalitarian` 如实标注 | 属**须拍板**项（保留/标注/退役三选一），未擅自决定 |
| 预设 `repro`↔`verify` 合并 | 改的是**用户的设置数据**（`settings.yaml`），须用户确认 |
| 新增 `dispute` 席 | 同上；且预设库是用户的资产，不由我代改 |
| `npm run build` / `typecheck` 全绿 | **预存的 7 个错误**（`members.ts`/`index.ts` 的宿主依赖版本漂移：`ContentBlock` 导出消失、`ctx.subagents` → `ctx.agents`）**在我改动前就存在**，已用 `git stash` 对照实证。修它属**独立任务**（涉及宿主 API 迁移），未混入本轮——**避免拆东墙补西墙** |
