# [项目事实] how-to · 组件接线验证流程

- 卡类型：how-to
- 溯源：test/mutate.mjs · 304 pass/18 PASS
- 源会话：session-4110fe62-2107-487a-a7b6-6e78eb7ae1e7
- 工作区：D:\lk\FF\dsh-plugin-roundtable

源级守卫不足（易恒真）→ ①用 react-dom/server 真渲染生产组件，断言输入框/按钮/占位/禁用态落在真 DOM ②两处渲染逐字比对证明共用 ③用 test/mutate.mjs 逐条变异实测每个守卫会转红。注意：CSS-module 导入在转码路径不可解析，须在测试 loader stub；断言按块收窄，全文件断言「无 textarea」会因同文件合法反馈框/驳回理由假红；变异锚点须真破坏（加无意义 __probe 不算）。

**追加（v0.2.49）**：变异判据已加严——`# fail > 0` 不足以判定，因为编译崩/别的测试连带红同样会让它 > 0（实测：把锚点改成语法错误，旧判据照样报 PASS）。现判据要求**失败块内部出现该变异的 `expect` 文案**，切块范围 = `not ok` 起至下一个 `ok`/`# ` 止。判据失效的代价是真实的：加严后立刻抓出 3 条 `expect` 写错（7/10/11）、1 条锚点失配（2）、1 条**恒真守卫**（21：只断言 `from_user:` 键存在，而 `from_user: false,` 同样含它）。当前规模：309 pass / 27 变异全 PASS。另：按键决策类守卫（如 IME 组合态）**在渲染面上不可观测**，必须提成纯函数直测，否则 SSR 断言全绿也能把守卫删掉。
