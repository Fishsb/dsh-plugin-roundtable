# [项目事实] decision · 空状态输入框即开会入口

- 卡类型：decision
- 溯源：b5bfe41 · v0.2.48
- 源会话：session-4110fe62-2107-487a-a7b6-6e78eb7ae1e7
- 工作区：D:\lk\FF\dsh-plugin-roundtable

无会议空状态窗口保留，另加与群聊共用的输入框（ChatComposer.tsx 两处引用，不写两份防漂移）。无会议时消息无处可落，定案：语义等同 `/roundtable <议题>`。新增端点 roundtable/steer，复用 steerCaptain 投递；不复用 runModeCommand（按命令语法解析，输入 off 会退模式而不转交）；先置模式再 steer（模式段属 system prompt，顺序反了会「议题已转交但主持人未按圆桌处理」）。无活 agent 时明确报错 has no live agent to steer。
