v0.2.61（下半）：群聊信息层级 + 角色预设头像与职能名 + 中英跟随语言

## 一、群聊（需求：参考正常群消息）

- 日期分隔改"今天/昨天"横线式（与"第 N 轮"胶囊刻意不同形，
  避免把轮次误读成时间点）
- 补发言类别徽标（检索 / 代思考）：真实 transcript 168 行里 157 speech
  + 11 retrieval，同形渲染会让"我去查了什么"被读成"我主张什么"
- 补工作项标记（44/168 行带 workItem）
- kind 与收件人解耦：原先用 kind==='speech' 同时决定"显示收件人"，
  是错的耦合（收件人是独立维度）
- 用户消息不再参与合并：user→user 连发会吞掉时间戳与「我」标签
- 长发言折叠（实测单条最长 10821 字、>800 字有 140/168 条）

## 二、角色预设（需求：头像 + 职能名，群聊与拓扑可见）

- RolePreset/Config/净化/WireRolePreset/WireNode 增 title(16) 与 avatar(8)，
  以及 presetId 透出（此前 host 内部有、未下发，UI 无法反查预设）
- 新增 preset-avatars.ts：24 字形内置集。选字形而非图片的理由是**快照载荷**
  —— snapshot.ts 自陈"16 会议 301KB"，而快照是 1Hz 轮询面
- 净化空值省略不落库：老数据零影响，且不把 rolePresets 每次全量重写
- 设置页：头像选择器 + 职能名输入；编辑时回填（漏了会把已设头像抹掉）
- 群聊成员表按 presetId 反查：昵称用职能名、头像用字形
  （原先一律 provider 品牌 —— 同 provider 多席会"长得一模一样"）

## 三、中英文跟随系统语言（需求）

宿主 locale 服务本就跟随系统（detectBrowserLocale 读 navigator.languages，
zh-CN 会退到子标签命中）；真缺口是插件自身 9 处硬编码中文 + 3 处写死 zh-CN：
- 全部接入 t()；localeTag 做成词条（t 是唯一随语言刷新的通道）
- 产物核验：硬编码中文残留 0、'zh-CN' 残留 0

## 四、角色预设入库（预设是项目的一部分）

experts.yaml / EXPERTS.md 由 scripts/export-experts.mjs 从宿主真实落盘直出。
重新生成：内容零丢失（28 席 → 28 席，id 全同，role 正文 3858 字逐字节一致），
补上了此前缺失的 reasoningEffort 字段。

## 证据

- 410/410 测试通过
- typecheck 仅剩既有环境错误（index.ts:16 解析到另一份 DSH，已复现确认与本批无关）
- 产物核验 + 部署链 Junction 哈希一致 + 热重载 [active]
- 字典 zh=266 / en=266 零差异
- 修复 test/effort-flow.test.mjs 的脆弱锚点（用会变的 UI 文案定位）；
  断言语义未变，未为变绿而削弱断言

## 未验证

真机像素：GUI 需登录态（401），无 CDP 通道。所有对比度均为按令牌链计算值。
