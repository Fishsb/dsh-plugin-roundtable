# 行尾归一（2026-09-23 立）
#
# ## 为什么需要这个文件
#
# 本机 `core.autocrlf=true`，而仓内**没有** `.gitattributes` ⇒ git 自行决定哪些
# 文件在工作区写成 CRLF。实测（148 个受控文件）：工作区 **13 个 CRLF / 111 个 LF**，
# 而 git **索引里存的却一律是 LF**（13/13 实测）。
#
# 这个"工作区形态不确定"的状态已经造成过一次**真实缺陷**（v0.2.55 修）：
# `test/mutate.mjs` 的 4 组变异探针（2/14/19/20）因锚点字面量是 LF 而工作区
# `src/rpc.ts` 是 CRLF ⇒ `includes()` 恒 false ⇒ 探针在「锚点没找到」处静默退出，
# **从来没在测任何东西**；而 `npm test` 不执行该文件 ⇒ 全仓 352 条测试**全绿**，
# 失效完全不可观测（典型「让失败不可观测」类缺陷）。
#
# ## 本文件做什么
#
# 把"源文件一律 LF"从**偶然**变成**契约**：不论 core.autocrlf 如何设置、
# 不论在 Windows/macOS/Linux 哪台机器上检出，工作区与索引都是 LF。
#
# ## 为什么它不改动任何既有内容（实测）
#
# `eol=lf` 只规定"检出时写成什么"，而索引侧本来就已是 LF（13/13 实测为 LF，
# `git check-attr text/eol` 全为 unspecified）⇒ 加本文件**不产生索引变更**。
# 校验：`git status` 在加本文件前后应只显示本次有意修改的文件。
#
# ## 边界（明确写出，避免被后人的通配规则覆盖）
#
# - 行尾敏感脚本：`.ps1` 在本仓当前就是 LF 且工作正常（PowerShell Core 支持 LF），
#   故显式声明为 LF，**把现状固定下来**而不是留成偶然。若将来加入 `.bat`/`.cmd`
#   （cmd.exe 对 LF 处理不良），须在下方单独为它们声明 `eol=crlf` —— 不要用 `*` 兜。

# 默认：文本自动识别 + 检出为 LF
* text=auto eol=lf

# 显式固化源文件与文档类型（与上一条冗余，但让"这些必须是 LF"可被 grep 到，
# 且不受未来 `*` 规则调整影响）
*.ts    text eol=lf
*.tsx   text eol=lf
*.mjs   text eol=lf
*.js    text eol=lf
*.json  text eol=lf
*.jsonl text eol=lf
*.md    text eol=lf
*.css   text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf

# PowerShell 脚本：本仓当前为 LF 且实测可用，固定之（见文件头「边界」）
*.ps1   text eol=lf

# 二进制件：禁止任何行尾转换（否则 git 会改写字节 ⇒ 产物损坏）
# 实测本仓二进制件 19 个：.txt 日志 12（含 NUL）、.png 5、.jpg 2
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.webp  binary
*.ico   binary
*.pdf   binary
*.zip   binary
*.tgz   binary
*.woff  binary
*.woff2 binary

# 例外：`*.txt` 一律按文本处理会弄坏 .audit/bench 下含 NUL 的抓取日志。
# git 的 `text=auto` 本来就会把含 NUL 的文件判为二进制，但这里显式收窄：
# 仅让普通文本 .txt 走 LF，含 NUL 者由 git 自动探测拦下（不做全局 text）。
*.txt   text=auto
